@src/  当前的页面中，特别是管理员编辑模型的有很多的地方需要优化

1. 模型库的模型过多的时候，面板没有滚动按钮，会导致这个组件和左下角的模型编辑面板产生了冲突，会造成遮挡。
2. 编辑模型的时候，使用鼠标拖拽的时候，会和页面的拖拽产生冲突，需要修复。管理员模式下视角转换必须通过鼠标右键，这里可以修改成为使用jikl进行视角的旋转，wasd进行前后左右平移，鼠标在管理员模式下只能对模型进行操作，无法拖拽页面
3. 编辑模型的时候，无法直接从属性面板中提交，随后修改模型位置，缩放等信息，此外，也无法编辑预设的AI提示词。这里必须修复。@EditorToolbar.tsx 
4. 画面最开始的6个柱子不太需要
5. 画面默认场景黑色的放置位置，有些小，应该能够随着模型的大小进行调整和渲染，这里似乎没有进行正确的加载@SceneEnvironment.tsx 
6. 在右下角选择场景的时候，无法选择自定义的场景内容。没有显示自定义的场景@SceneSelector.tsx ，是因为data中没有记录吗？能不能更新这里的代码，提示词等内容应该也要同步更新到配置文件，而不是只能使用默认的@scenes.ts @aiService.ts @sceneData.ts 
7. 这里的开始探索页面太长了，需要滚动才能点击进入探索按钮，可以适当调整布局@Instructions.tsx 
